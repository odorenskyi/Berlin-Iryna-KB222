#include <iostream>
#include <cmath>
#include <locale>
#include "Berlin_lib.h"

using namespace std;

int main()
{
    setlocale(LC_ALL, "ukr");
    int x, y, z;
    char a, b;
cout << "¬вед≥ть числа: x, y, z: ";
    cin >> x >> y >> z;

    cout << "¬вед≥ть символи a, b: ";
    cin >> a >> b;
}
